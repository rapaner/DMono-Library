# Руководство по развёртыванию и релизам

## 📦 Процесс релиза

### Подготовка к релизу

1. **Обновите версию приложения**
   
   Отредактируйте `Library.csproj`:
   ```xml
   <ApplicationDisplayVersion>1.14</ApplicationDisplayVersion>
   <ApplicationVersion>14</ApplicationVersion>
   ```

2. **Обновите CHANGELOG.md**
   
   Добавьте описание изменений в новой версии:
   ```markdown
   ## [1.14] - 2025-10-16
   ### Добавлено
   - Новая функция X
   
   ### Исправлено
   - Баг Y
   ```

3. **Проверьте изменения локально**
   ```powershell
   dotnet build Library.csproj -c Release -f net9.0-android
   ```

4. **Закоммитьте изменения**
   ```powershell
   git add Library.csproj CHANGELOG.md
   git commit -m "Release version 1.14"
   git push
   ```

### Создание релиза

1. **Создайте и запушьте тег**
   ```powershell
   git tag v1.14
   git push origin v1.14
   ```

2. **Автоматическая сборка**
   - GitHub Actions автоматически соберёт и подпишет APK
   - Создаст GitHub Release с APK файлом
   - Процесс занимает ~10-15 минут

3. **Проверьте релиз**
   - Перейдите в GitHub Releases
   - Скачайте и протестируйте APK
   - При необходимости отредактируйте описание релиза

### Горячие исправления (hotfix)

Для срочных исправлений:

```powershell
# Создайте ветку hotfix
git checkout -b hotfix/critical-fix

# Внесите исправления
# ...

# Обновите версию (например, 1.13 -> 1.13.1)
# Закоммитьте
git add .
git commit -m "Hotfix: critical bug fix"

# Создайте PR или слейте напрямую в main
git checkout main
git merge hotfix/critical-fix
git push

# Создайте тег
git tag v1.13.1
git push origin v1.13.1
```

## 🔄 Обновление зависимостей

### Обновление .NET MAUI

```powershell
# Проверьте доступные версии
dotnet workload search maui

# Обновите workload
dotnet workload update

# Обновите пакеты в проектах
dotnet add package Microsoft.Maui.Controls --version 9.0.10
```

### Обновление NuGet пакетов

```powershell
# Просмотр устаревших пакетов
dotnet list package --outdated

# Обновление всех пакетов
dotnet outdated -u

# Обновление конкретного пакета
dotnet add package YandexDisk.Client --version 1.3.28
```

## 🚀 Публикация в Google Play

### Первая публикация

1. **Создайте приложение в Google Play Console**
   - Перейдите в [Google Play Console](https://play.google.com/console)
   - Создайте новое приложение
   - Заполните информацию о приложении

2. **Настройте App Signing**
   - Internal testing → Upload → App signing by Google Play
   - Или используйте свой keystore

3. **Загрузите APK/AAB**
   - Скачайте signed APK из GitHub Release
   - Загрузите в Internal testing track
   - Заполните обязательные формы

4. **Создайте релиз**
   - Добавьте release notes
   - Отправьте на review

### Обновления приложения

1. **Увеличьте version code** в `Library.csproj`:
   ```xml
   <ApplicationVersion>14</ApplicationVersion> <!-- Было 13 -->
   ```

2. **Создайте релиз через GitHub Actions** (см. выше)

3. **Загрузите новый APK** в Google Play Console

4. **Обновите release notes** и отправьте на review

## 📊 Мониторинг релизов

### Проверка статуса сборки

1. **GitHub Actions**
   ```
   Репозиторий → Actions → Android Build and Release
   ```

2. **Просмотр логов**
   - Откройте конкретный workflow run
   - Разверните шаги для просмотра деталей

### Скачивание артефактов

**Через Web интерфейс:**
- Actions → Workflow run → Artifacts

**Через GitHub CLI:**
```powershell
# Установите GitHub CLI
winget install GitHub.cli

# Скачайте последний артефакт
gh run download --name android-release-signed-apk
```

## 🔐 Управление ключами подписи

### Ротация keystore (если скомпрометирован)

⚠️ **ВНИМАНИЕ:** Это сложная процедура для опубликованных приложений!

1. **Для новых приложений:**
   ```powershell
   # Создайте новый keystore
   keytool -genkeypair -v -keystore library-new.keystore -alias myappalias -keyalg RSA -keysize 2048 -validity 10000
   
   # Обновите GitHub Secrets
   [Convert]::ToBase64String([IO.File]::ReadAllBytes("library-new.keystore"))
   ```

2. **Для опубликованных приложений:**
   - Если используете Google Play App Signing - можно обновить upload key
   - Если используете собственный keystore - НЕ МЕНЯЙТЕ (невозможно обновить приложение)

### Смена паролей

```powershell
# Изменить пароль keystore
keytool -storepasswd -keystore library.keystore

# Изменить пароль ключа
keytool -keypasswd -alias myappalias -keystore library.keystore
```

После смены паролей обновите GitHub Secrets!

## 🧪 Тестирование релизов

### Внутреннее тестирование

1. **Скачайте APK** из GitHub Releases

2. **Установите на устройство:**
   ```powershell
   adb install -r path/to/app.apk
   ```

3. **Проверьте основные функции:**
   - Запуск приложения
   - Основные функции
   - Синхронизация с Yandex Disk
   - Тема оформления

### Альфа/Бета тестирование в Google Play

1. **Создайте закрытый трек** (Closed testing)
2. **Добавьте тестеров** по email
3. **Загрузите APK** в этот трек
4. **Соберите feedback** перед production релизом

## 📝 Чек-лист релиза

Перед каждым релизом:

- [ ] Увеличен version code
- [ ] Обновлен CHANGELOG.md
- [ ] Локально протестирована сборка
- [ ] Все тесты проходят
- [ ] Нет критических bugs
- [ ] Обновлена документация (если нужно)
- [ ] Создан и запушен тег
- [ ] GitHub Actions успешно завершился
- [ ] Скачан и протестирован APK из Release
- [ ] Release notes заполнены

## 🛠️ Полезные команды

### Проверка версии приложения

```powershell
# Из APK файла
aapt dump badging app.apk | Select-String "versionCode|versionName"
```

### Проверка подписи APK

```powershell
# Информация о подписи
jarsigner -verify -verbose -certs app-signed.apk

# Fingerprints
keytool -list -printcert -jarfile app-signed.apk
```

### Просмотр размера APK

```powershell
# Размер файла
Get-Item app.apk | Select-Object Name, Length

# Анализ содержимого (требует Android Studio)
# Откройте APK в Android Studio → Build → Analyze APK
```

## 📚 Дополнительные ресурсы

- [Google Play Console Help](https://support.google.com/googleplay/android-developer)
- [Android App Bundles](https://developer.android.com/guide/app-bundle)
- [.NET MAUI Deployment](https://docs.microsoft.com/en-us/dotnet/maui/deployment/)
- [Semantic Versioning](https://semver.org/)

