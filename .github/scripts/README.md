# Скрипты для управления Android Keystore

Эта папка содержит PowerShell скрипты для упрощения работы с Android keystore и настройки GitHub Actions.

## 📋 Доступные скрипты

### 1. create-keystore.ps1

Создаёт новый Android keystore для подписи приложений.

**Использование:**
```powershell
# С параметрами по умолчанию
.\.github\scripts\create-keystore.ps1

# С пользовательскими параметрами
.\.github\scripts\create-keystore.ps1 -KeystoreName "my-app.keystore" -Alias "mykey" -Validity 10000
```

**Параметры:**
- `KeystoreName` - имя файла keystore (по умолчанию: `library.keystore`)
- `Alias` - алиас ключа (по умолчанию: `myappalias`)
- `Validity` - срок действия в днях (по умолчанию: `10000` ≈ 27 лет)

**Что делает:**
- ✅ Проверяет наличие Java keytool
- ✅ Создаёт резервную копию существующего keystore
- ✅ Запрашивает необходимую информацию
- ✅ Создаёт новый keystore
- ✅ Выводит важную информацию для сохранения

### 2. convert-keystore.ps1

Конвертирует keystore в Base64 для использования в GitHub Secrets.

**Использование:**
```powershell
# С параметрами по умолчанию
.\.github\scripts\convert-keystore.ps1

# С пользовательскими параметрами
.\.github\scripts\convert-keystore.ps1 -KeystorePath "library.keystore" -OutputFile "keystore.txt"
```

**Параметры:**
- `KeystorePath` - путь к keystore файлу (по умолчанию: `library.keystore`)
- `OutputFile` - файл для сохранения Base64 (по умолчанию: `keystore_base64.txt`)

**Что делает:**
- ✅ Читает keystore файл
- ✅ Конвертирует в Base64
- ✅ Сохраняет в текстовый файл
- ✅ Предлагает скопировать в буфер обмена
- ✅ Проверяет .gitignore

## 🚀 Быстрый старт

### Первая настройка (новый keystore)

```powershell
# 1. Создайте новый keystore
.\.github\scripts\create-keystore.ps1

# 2. Конвертируйте в Base64
.\.github\scripts\convert-keystore.ps1

# 3. Добавьте секреты в GitHub (см. .github/SETUP_SECRETS.md)
```

### Использование существующего keystore

```powershell
# 1. Конвертируйте существующий keystore
.\.github\scripts\convert-keystore.ps1 -KeystorePath "path/to/your.keystore"

# 2. Добавьте секреты в GitHub (см. .github/SETUP_SECRETS.md)
```

## ⚠️ Важные замечания

### Безопасность

- 🔒 **НИКОГДА** не коммитьте keystore файлы в Git
- 🔒 **НИКОГДА** не коммитьте файлы с паролями или Base64
- 🔒 Убедитесь, что `.gitignore` содержит:
  ```
  *.keystore
  *.jks
  keystore_base64.txt
  ```

### Резервное копирование

- 💾 Сделайте несколько резервных копий keystore
- 💾 Храните в безопасном месте (не только на одном компьютере)
- 💾 Используйте менеджер паролей для хранения паролей
- 💾 Рассмотрите зашифрованное облачное хранилище

### Google Play

- 📱 Если приложение уже опубликовано - **НЕ МЕНЯЙТЕ KEYSTORE!**
- 📱 Для обновлений нужен **тот же самый** keystore
- 📱 Рассмотрите использование [Google Play App Signing](https://developer.android.com/studio/publish/app-signing#app-signing-google-play)

## 🔧 Требования

### Для create-keystore.ps1:
- Java JDK (содержит `keytool`)
- PowerShell 5.1 или выше

### Для convert-keystore.ps1:
- PowerShell 5.1 или выше
- Существующий keystore файл

## 📚 Дополнительная документация

- [SETUP_SECRETS.md](../SETUP_SECRETS.md) - Настройка GitHub Secrets
- [DEPLOYMENT_GUIDE.md](../DEPLOYMENT_GUIDE.md) - Руководство по развёртыванию
- [REMOVE_KEYSTORE.md](../REMOVE_KEYSTORE.md) - Удаление keystore из Git

## 🆘 Помощь

### keytool не найден

```powershell
# Установите Java JDK
winget install Oracle.JDK.21

# Или скачайте вручную:
# https://www.oracle.com/java/technologies/downloads/
# https://adoptium.net/
```

### Ошибка при выполнении скриптов

Если PowerShell блокирует выполнение:

```powershell
# Временно разрешить выполнение (для текущей сессии)
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process

# Затем запустите скрипт
.\.github\scripts\create-keystore.ps1
```

### Забыли пароль keystore

К сожалению, если вы забыли пароль keystore, его **невозможно восстановить**.

Варианты:
1. Если приложение не опубликовано - создайте новый keystore
2. Если опубликовано с Google Play App Signing - свяжитесь с Google Support
3. Если опубликовано без App Signing - придётся создать новое приложение

## 💡 Полезные команды

### Просмотр информации о keystore

```powershell
keytool -list -v -keystore library.keystore
```

### Проверка срока действия

```powershell
keytool -list -v -keystore library.keystore | Select-String "Valid"
```

### Экспорт сертификата

```powershell
keytool -export -alias myappalias -keystore library.keystore -file certificate.crt
```

### Получение SHA-1 fingerprint

```powershell
keytool -list -v -keystore library.keystore | Select-String "SHA1"
```

## 🤝 Содействие

Если вы нашли баг или хотите улучшить скрипты, создайте issue или pull request!

