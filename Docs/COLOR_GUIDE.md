# Руководство по цветам приложения

> 💡 **Приложение поддерживает светлую и темную темы!** См. [THEME_SUPPORT.md](THEME_SUPPORT.md) для подробностей.

## Основные цвета

### Светлая тема (Light Theme)

#### Цвета бренда
- **PrimaryColor / Primary**: `#512BD4` - Основной фиолетовый цвет
- **SecondaryColor / Secondary**: `#DFD8F7` - Светло-фиолетовый цвет
- **TertiaryColor / Tertiary**: `#2B0B98` - Темно-фиолетовый цвет
- **AccentColor**: `#E0E0E0` - Акцентный серый цвет

#### Фоновые цвета
- **PageBackgroundColor**: `#F5F5F5` - Светло-серый фон страниц
- **CardBackgroundColor**: `#FFFFFF` - Белый фон карточек

#### Текстовые цвета
- **PrimaryTextColor**: `#000000` - Черный цвет для основного текста
- **SecondaryTextColor**: `#666666` - Серый цвет для вторичного текста

### Темная тема (Dark Theme)

#### Цвета бренда
- **PrimaryColor / Primary**: `#8B6FE8` - Светло-фиолетовый цвет
- **SecondaryColor / Secondary**: `#4A3A6E` - Темно-фиолетовый цвет
- **TertiaryColor / Tertiary**: `#6B50D4` - Средний фиолетовый
- **AccentColor**: `#424242` - Темно-серый цвет

#### Фоновые цвета
- **PageBackgroundColor**: `#121212` - Почти черный фон страниц
- **CardBackgroundColor**: `#1E1E1E` - Темно-серый фон карточек

#### Текстовые цвета
- **PrimaryTextColor**: `#FFFFFF` - Белый цвет для основного текста
- **SecondaryTextColor**: `#B0B0B0` - Светло-серый для вторичного текста

### Статусные цвета
- **SuccessColor**: `#28A745` - Зеленый для успешных операций
- **WarningColor**: `#FFC107` - Желтый для предупреждений
- **ErrorColor**: `#DC3545` - Красный для ошибок

### Оттенки серого
- **Gray100**: `#F5F5F5` - Самый светлый
- **Gray200**: `#EEEEEE`
- **Gray300**: `#E0E0E0`
- **Gray400**: `#BDBDBD`
- **Gray500**: `#9E9E9E`
- **Gray600**: `#757575` - Средний серый (используется для вторичного текста)
- **Gray900**: `#212121` - Самый темный

## Использование в XAML

### Для Label
```xml
<Label Text="Основной заголовок" 
       TextColor="{DynamicResource PrimaryTextColor}" />

<Label Text="Вторичный текст" 
       TextColor="{DynamicResource SecondaryTextColor}" />
```

### Для Button
```xml
<!-- Основная кнопка -->
<Button Text="Сохранить" 
        BackgroundColor="{DynamicResource PrimaryColor}" 
        TextColor="White" />

<!-- Вторичная кнопка -->
<Button Text="Отмена" 
        BackgroundColor="{DynamicResource SecondaryColor}" 
        TextColor="{DynamicResource PrimaryTextColor}" />
```

### Для Entry/Editor
```xml
<Entry Placeholder="Введите текст"
       TextColor="{DynamicResource PrimaryTextColor}"
       PlaceholderColor="{DynamicResource SecondaryTextColor}" />
```

### Для Frame/Border
```xml
<Frame BackgroundColor="{DynamicResource CardBackgroundColor}" 
       CornerRadius="15" 
       Padding="20" 
       HasShadow="True">
    <!-- Содержимое -->
</Frame>
```

## Глобальные стили

Для следующих элементов цвета текста установлены автоматически:
- `Label` - `PrimaryTextColor`
- `Button` - `White`
- `Entry` - `PrimaryTextColor`
- `Editor` - `PrimaryTextColor`
- `Picker` - `PrimaryTextColor`
- `DatePicker` - `PrimaryTextColor`
- `TimePicker` - `PrimaryTextColor`

## Рекомендации

1. **ВСЕГДА используйте DynamicResource** вместо StaticResource для цветов - это критически важно для поддержки тем!
   ```xml
   <!-- Правильно ✅ -->
   <Label TextColor="{DynamicResource PrimaryTextColor}" />
   
   <!-- Неправильно ❌ -->
   <Label TextColor="Black" />
   <Label TextColor="{StaticResource PrimaryTextColor}" />
   ```

2. **Контрастность**: На фоне карточки (`CardBackgroundColor`) всегда используйте `PrimaryTextColor` или `SecondaryTextColor`

3. **Кнопки**: 
   - На цветном фоне (`PrimaryColor`, `SuccessColor`, etc.) - белый текст
   - На светлом/темном фоне (`SecondaryColor`, `AccentColor`) - `PrimaryTextColor`

4. **Не используйте жестко заданные цвета** (например, `TextColor="Black"`) - используйте ресурсы для единообразия

5. **Тестирование тем**: Всегда проверяйте UI как в светлой, так и в темной теме

## Поддержка тем

Приложение автоматически переключается между светлой и темной темой в зависимости от системных настроек.

Подробнее см. [THEME_SUPPORT.md](THEME_SUPPORT.md)

