# OAuth авторизация в приложении

## ✅ Реализовано

В приложение добавлена полноценная OAuth авторизация через браузер для получения токена Яндекс Диска.

## 🎯 Как это работает

### Процесс авторизации:

1. Пользователь нажимает кнопку **"🔐 Войти через Яндекс"**
2. Открывается системный браузер с страницей авторизации Яндекс
3. Пользователь входит в свой аккаунт (если не вошел)
4. Яндекс запрашивает разрешение на доступ к Яндекс Диску
5. После разрешения браузер перенаправляет на callback URL приложения
6. Приложение автоматически получает токен и сохраняет его
7. Готово! Пользователь подключен к Яндекс Диску

### Технические детали:

- Используется **WebAuthenticator** из .NET MAUI Essentials
- Callback URL: `ru.rapaner.library://oauth`
- Client ID: `23cabbbdc6cd418abb4b39c32c41195d` (публичный тестовый)
- Токен автоматически извлекается из URL и сохраняется

## 📦 Созданные компоненты

### 1. YandexOAuthService (`Services/YandexOAuthService.cs`)

Сервис для OAuth авторизации:

```csharp
public class YandexOAuthService
{
    // Запустить процесс авторизации
    public async Task<string?> AuthenticateAsync()
    
    // Проверить поддержку WebAuthenticator
    public bool IsSupported()
}
```

### 2. Настройка Android (`Platforms/Android/AndroidManifest.xml`)

Добавлена activity для обработки callback:

```xml
<activity android:name="microsoft.maui.essentials.WebAuthenticatorCallbackActivity" android:exported="true">
    <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="ru.rapaner.library" android:host="oauth" />
    </intent-filter>
</activity>
```

### 3. Настройка Windows (`Platforms/Windows/Package.appxmanifest`)

Добавлен protocol handler:

```xml
<Extensions>
    <uap:Extension Category="windows.protocol">
        <uap:Protocol Name="ru.rapaner.library">
            <uap:DisplayName>Library OAuth</uap:DisplayName>
        </uap:Protocol>
    </uap:Extension>
</Extensions>
```

### 4. Обновленный UI (`Views/YandexDiskPage.xaml`)

Добавлена большая заметная кнопка:

```xml
<Button Text="🔐 Войти через Яндекс" 
        Clicked="OnGetTokenClicked"
        BackgroundColor="{StaticResource Primary}"
        FontSize="16"
        FontAttributes="Bold"
        HeightRequest="50"/>
```

## 🚀 Использование

### Для пользователя:

1. Откройте приложение
2. Перейдите в **Настройки** → **☁️ Яндекс Диск**
3. Нажмите кнопку **"🔐 Войти через Яндекс"**
4. В открывшемся браузере войдите в аккаунт Яндекс
5. Разрешите доступ к Яндекс Диску
6. Приложение автоматически получит токен и подключится

### Для разработчика:

```csharp
// Получить сервис через DI
var oauthService = // из конструктора

// Запустить авторизацию
var token = await oauthService.AuthenticateAsync();

if (!string.IsNullOrEmpty(token))
{
    // Токен получен успешно
    _yandexDiskService.SetOAuthToken(token);
}
```

## 🔧 Настройка собственного Client ID

Если вы хотите использовать собственный Client ID:

### 1. Зарегистрируйте приложение на Яндекс OAuth

1. Перейдите на https://oauth.yandex.ru/
2. Нажмите **"Зарегистрировать новое приложение"**
3. Заполните форму:
   - **Название**: "Моя Библиотека" (или любое другое)
   - **Платформы**: выберите нужные (Android, Windows)
   - **Callback URL**: `ru.rapaner.library://oauth`
   - **Права доступа**: отметьте "Яндекс.Диск" → "Доступ к приложению" и "Запись"

### 2. Получите Client ID

После регистрации скопируйте Client ID из настроек приложения.

### 3. Обновите код

Замените Client ID в `Services/YandexOAuthService.cs`:

```csharp
private const string ClientId = "ВАШ_CLIENT_ID";
```

### 4. (Опционально) Измените callback URL

Если вы хотите использовать другой callback URL:

1. Обновите `CallbackScheme` в `YandexOAuthService.cs`
2. Обновите `android:scheme` в `AndroidManifest.xml`
3. Обновите `Name` в `Package.appxmanifest`

## 🔐 Безопасность

### Что безопасно:

✅ **OAuth токен получается через официальный OAuth flow**
✅ **Токен хранится локально на устройстве пользователя**
✅ **Все запросы к API используют HTTPS**
✅ **Токен имеет ограниченные права (только Яндекс Диск)**
✅ **Пользователь явно разрешает доступ**

### Важные замечания:

⚠️ **Client ID в коде** - это нормально для мобильных приложений (OAuth 2.0 для публичных клиентов)
⚠️ **Нет Client Secret** - мобильные приложения не могут безопасно хранить секреты
⚠️ **Токен без refresh** - используется Implicit Flow, токен действует долго, но может потребоваться повторная авторизация

## 📱 Поддержка платформ

| Платформа | Поддержка | Примечания |
|-----------|-----------|------------|
| Android | ✅ Да | Полная поддержка через Custom Tabs |
| Windows | ✅ Да | Полная поддержка через системный браузер |
| iOS | ✅ Да | Требуется настройка в Info.plist |
| macOS | ✅ Да | Требуется настройка в Info.plist |

### Настройка для iOS/macOS

Если вы планируете поддержку iOS/macOS, добавьте в `Info.plist`:

```xml
<key>CFBundleURLTypes</key>
<array>
    <dict>
        <key>CFBundleURLSchemes</key>
        <array>
            <string>ru.rapaner.library</string>
        </array>
        <key>CFBundleURLName</key>
        <string>Library OAuth</string>
    </dict>
</array>
```

## 🎨 Пользовательский интерфейс

### Обновленная страница авторизации:

- ✅ Большая заметная кнопка "🔐 Войти через Яндекс"
- ✅ Альтернативный способ (ручной ввод токена)
- ✅ Обновленная инструкция с двумя способами
- ✅ Индикатор загрузки во время авторизации
- ✅ Информативные сообщения об успехе/ошибке

## 🐛 Обработка ошибок

Сервис обрабатывает следующие ситуации:

- ✅ Пользователь отменил авторизацию
- ✅ Ошибка сети
- ✅ Неверный токен
- ✅ Таймаут авторизации
- ✅ Браузер не поддерживается

## 📊 Преимущества OAuth flow

### По сравнению с ручным вводом токена:

1. **Удобство** - пользователю не нужно копировать токен
2. **Безопасность** - токен не проходит через буфер обмена
3. **Надежность** - меньше ошибок при вводе
4. **Профессионализм** - стандартный способ авторизации
5. **Автоматизация** - весь процесс автоматический

## 🔄 Обновление токена

Яндекс OAuth использует Implicit Flow, который не предоставляет refresh token. Когда токен истечет:

1. Пользователь увидит ошибку подключения
2. Нужно будет повторно нажать "🔐 Войти через Яндекс"
3. Если пользователь уже авторизован в Яндексе, процесс будет быстрым

### Альтернатива (для будущей реализации):

Можно реализовать Authorization Code Flow с PKCE для получения refresh token, но это требует:
- Серверной части для обмена кода на токен
- Более сложной настройки
- Хранения refresh token

## 📝 Примеры использования

### Базовая авторизация:

```csharp
var oauthService = new YandexOAuthService();
var token = await oauthService.AuthenticateAsync();

if (!string.IsNullOrEmpty(token))
{
    Console.WriteLine("Авторизация успешна!");
}
```

### С обработкой ошибок:

```csharp
try
{
    var token = await oauthService.AuthenticateAsync();
    
    if (!string.IsNullOrEmpty(token))
    {
        // Сохраняем токен
        _yandexDiskService.SetOAuthToken(token);
        
        // Проверяем токен
        var diskInfo = await _yandexDiskService.GetDiskInfoAsync();
        
        // Сохраняем в настройках
        var settings = new YandexDiskSettings { OAuthToken = token };
        _settingsService.SaveYandexDiskSettings(settings);
    }
}
catch (Exception ex)
{
    Console.WriteLine($"Ошибка: {ex.Message}");
}
```

## 🎉 Результат

Теперь пользователи могут:

- ✅ Авторизоваться одним нажатием кнопки
- ✅ Не копировать токены вручную
- ✅ Использовать стандартный OAuth flow
- ✅ Быстро переавторизоваться при необходимости

Процесс авторизации стал:
- 🚀 Быстрее
- 🎯 Проще
- 🔐 Безопаснее
- 💎 Профессиональнее

## 📚 Дополнительные ресурсы

- [Документация Яндекс OAuth](https://yandex.ru/dev/id/doc/ru/)
- [.NET MAUI WebAuthenticator](https://learn.microsoft.com/en-us/dotnet/maui/platform-integration/communication/authentication)
- [OAuth 2.0 для мобильных приложений](https://oauth.net/2/native-apps/)
