# Интеграция с Яндекс Диском

## Описание

В проект добавлена полная интеграция с Яндекс Диском для резервного копирования и восстановления базы данных библиотеки.

## Установленные компоненты

### NuGet пакеты
- **YandexDisk.Client** (v1.3.27) - официальная библиотека для работы с API Яндекс Диска

### Созданные файлы

1. **Services/YandexDiskService.cs** - основной сервис для работы с Яндекс Диском
2. **Services/SettingsService.cs** - сервис для сохранения настроек приложения
3. **Models/YandexDiskSettings.cs** - модель настроек Яндекс Диска
4. **Views/YandexDiskPage.xaml** - страница управления интеграцией
5. **Views/YandexDiskPage.xaml.cs** - код-бихайнд страницы

## Функциональность

### YandexDiskService

Основной сервис предоставляет следующие возможности:

- ✅ **Авторизация** через OAuth токен
- ✅ **Загрузка файлов** на Яндекс Диск
- ✅ **Скачивание файлов** с Яндекс Диска
- ✅ **Проверка существования файлов**
- ✅ **Получение списка файлов** в директории
- ✅ **Создание директорий**
- ✅ **Удаление файлов**
- ✅ **Резервное копирование базы данных**
- ✅ **Восстановление базы данных** из резервной копии
- ✅ **Получение списка резервных копий**
- ✅ **Информация о диске** (свободное/занятое место)

### YandexDiskPage

Страница управления предоставляет:

- 📊 **Статус подключения** к Яндекс Диску
- 🔐 **Авторизация** через OAuth токен
- 💾 **Создание резервных копий** вручную
- 📥 **Восстановление** из резервной копии
- ⚙️ **Автоматическое резервное копирование** (настройка частоты)
- 📋 **Список резервных копий** на Яндекс Диске
- ℹ️ **Инструкции** по получению OAuth токена

## Как получить OAuth токен

### Вариант 1: Использование готового приложения (рекомендуется для тестирования)

Для быстрого тестирования можно использовать тестовый токен:

1. Перейдите на [Полигон Яндекс OAuth](https://yandex.ru/dev/disk/poligon/)
2. Нажмите "Получить токен"
3. Войдите в свой аккаунт Яндекс
4. Разрешите доступ к Яндекс Диску
5. Скопируйте полученный токен

⚠️ **Внимание**: Токены с полигона имеют ограниченный срок действия!

### Вариант 2: Регистрация собственного приложения (для продакшена)

1. **Зарегистрируйте приложение**:
   - Перейдите на https://oauth.yandex.ru/
   - Нажмите "Зарегистрировать новое приложение"
   - Заполните форму:
     - Название: "Моя Библиотека" (или любое другое)
     - Платформа: выберите подходящую (Android/Windows)
     - Права доступа: отметьте "Яндекс.Диск" → "Доступ к приложению" и "Запись"

2. **Получите Client ID**:
   - После регистрации скопируйте Client ID

3. **Настройте OAuth URL**:
   - Для Android: используйте схему `your-app://oauth`
   - Для Windows: используйте `http://localhost:PORT/callback`

4. **Реализуйте OAuth flow**:
   ```
   https://oauth.yandex.ru/authorize?response_type=token&client_id=YOUR_CLIENT_ID
   ```

5. **Получите токен** из redirect URL после авторизации

### Вариант 3: Упрощенный способ (текущая реализация)

В текущей версии приложения:

1. Откройте приложение
2. Перейдите в **Настройки** → **☁️ Яндекс Диск**
3. Нажмите **"Получить OAuth токен"** для просмотра инструкций
4. Получите токен через полигон или зарегистрированное приложение
5. Вставьте токен в поле ввода
6. Нажмите **"Сохранить токен"**

## Использование

### Создание резервной копии

```csharp
// В коде
var yandexDiskService = // получить из DI
var dbPath = Path.Combine(FileSystem.AppDataDirectory, "library.db");
bool success = await yandexDiskService.BackupDatabaseAsync(dbPath);
```

Или через UI:
1. Откройте страницу "Яндекс Диск"
2. Нажмите "Создать резервную копию"

Резервные копии сохраняются в папку `/Library_Backups/` на Яндекс Диске с именем формата: `library_YYYYMMDD_HHmmss.db`

### Восстановление из резервной копии

```csharp
// В коде
var remotePath = "/Library_Backups/library_20241005_120000.db";
var localPath = Path.Combine(FileSystem.AppDataDirectory, "library.db");
bool success = await yandexDiskService.RestoreDatabaseAsync(remotePath, localPath);
```

Или через UI:
1. Откройте страницу "Яндекс Диск"
2. Нажмите "Обновить" для загрузки списка резервных копий
3. Выберите нужную резервную копию из списка
4. Нажмите "Восстановить из резервной копии"
5. Подтвердите действие

⚠️ **Внимание**: Приложение будет перезапущено после восстановления!

### Автоматическое резервное копирование

1. Откройте страницу "Яндекс Диск"
2. Включите переключатель "Автоматическое резервное копирование"
3. Установите частоту в днях (по умолчанию 7)
4. Нажмите "Сохранить"

> **Примечание**: Автоматическое резервное копирование требует дополнительной реализации фонового сервиса.

## Структура данных на Яндекс Диске

```
Яндекс Диск/
└── Library_Backups/
    ├── library_20241005_120000.db
    ├── library_20241004_180000.db
    └── library_20241003_090000.db
```

## Настройки

Настройки хранятся в `Preferences` (локальное хранилище):

```csharp
public class YandexDiskSettings
{
    public string? OAuthToken { get; set; }
    public bool AutoBackupEnabled { get; set; }
    public int AutoBackupFrequencyDays { get; set; } = 7;
    public DateTime? LastBackupDate { get; set; }
    public int MaxBackupsToKeep { get; set; } = 10;
}
```

## Безопасность

⚠️ **Важные замечания по безопасности**:

1. **OAuth токен хранится локально** в `Preferences` - это безопасно для мобильных приложений
2. **Не храните токен в исходном коде** или публичных репозиториях
3. **Используйте HTTPS** для всех запросов к API (библиотека делает это автоматически)
4. **Токен имеет ограниченные права** - только доступ к Яндекс Диску
5. **Рекомендуется** реализовать полноценный OAuth flow для продакшена

## Ограничения

- Токен с полигона имеет ограниченный срок действия
- API Яндекс Диска имеет лимиты на количество запросов
- Размер файлов для загрузки ограничен квотой пользователя

## Дальнейшие улучшения

Возможные улучшения:

1. ✨ **Полноценный OAuth flow** с автоматическим получением токена
2. 🔄 **Фоновая синхронизация** для автоматического резервного копирования
3. 🗑️ **Автоматическая очистка** старых резервных копий
4. 📊 **Прогресс загрузки/скачивания** файлов
5. 🔐 **Шифрование** резервных копий перед загрузкой
6. 📱 **Push-уведомления** о статусе резервного копирования
7. ☁️ **Синхронизация между устройствами**

## Примеры использования

### Проверка авторизации

```csharp
if (_yandexDiskService.IsAuthorized)
{
    // Пользователь авторизован
}
```

### Получение информации о диске

```csharp
var diskInfo = await _yandexDiskService.GetDiskInfoAsync();
Console.WriteLine($"Всего: {diskInfo.TotalSpace} байт");
Console.WriteLine($"Использовано: {diskInfo.UsedSpace} байт");
```

### Загрузка произвольного файла

```csharp
var localPath = "/path/to/local/file.txt";
var remotePath = "/MyFolder/file.txt";
bool success = await _yandexDiskService.UploadFileAsync(localPath, remotePath);
```

### Скачивание произвольного файла

```csharp
var remotePath = "/MyFolder/file.txt";
var localPath = "/path/to/save/file.txt";
bool success = await _yandexDiskService.DownloadFileAsync(remotePath, localPath);
```

## Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте правильность OAuth токена
2. Убедитесь, что у приложения есть права доступа к Яндекс Диску
3. Проверьте подключение к интернету
4. Посмотрите логи в Debug консоли

## Ссылки

- [Документация API Яндекс Диска](https://yandex.ru/dev/disk/api/reference/overview.html)
- [YandexDisk.Client на GitHub](https://github.com/raidenyn/yandexdisk.client)
- [OAuth Яндекс](https://yandex.ru/dev/id/doc/ru/)
